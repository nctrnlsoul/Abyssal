# What the PUBLIC console actually needs, and nothing else.
#
# Proven, not assumed: scripts/import_graph.py exercises every route in a
# fresh interpreter and asserts that google-adk, pymupdf and numpy are never
# reached. A public unauthenticated URL has no business loading an agent
# framework, and a suggested Dockerfile that installed the full requirements.txt
# would have put the whole ADK tree on that surface for no reason.
#
# FULLY pinned, transitive dependencies included, so a rebuild the night before
# a deadline cannot resolve a different starlette major than the one tested.
fastapi==0.141.1
uvicorn==0.46.0
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
h11==0.16.0
idna==3.19
pydantic==2.13.4
pydantic_core==2.46.4
sniffio==1.3.1
starlette==1.6.0
typing-inspection==0.4.4
typing_extensions==4.16.0
