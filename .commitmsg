Apply subagent verification pass: 4 must-fixes, wording honesty, CSS sweep

- Fix ReferenceError on the /api/run failure path (fail() was deleted with
  the tool pane); status reads "no recording" and controls disable
- Remove orphan </div> and vestigial #artifact-note that mis-nested the
  advisory, hydrophone band and footer
- Palette: new well #04070A and accent-well #1295A2 tokens; .trace-t was
  4.09:1 on the well, now 5.62:1; five new pinned contrast pairs; all
  hardcoded #04070A converted to var(--well)
- Americanize API-served strings (Unrecognized, cells-per-liter, map color)
  in synthesis.py and the recorded transcript; new test covers synthesise()
  output on both branches plus the recorded JSON
- Waveform: peak_at_seconds is bucket-center (16.0s, was bucket-start
  15.9s); labels read "peak ~16.0s" and "(ROUGHLY UNDER 500 HZ)" since the
  16-tap filter's -3dB corner is 443.7 Hz
- Header sub: "the live NOAA map" -> "NOAA's public red tide map"
- Close replay-during-sweep rAF race with a generation counter; reset also
  clears the pending sweep timer and playhead
- Map caption returns to the awaiting text when markers clear on replay
- Hide the empty advisory box pre-replay (.advisory:empty)
- HEAD / returns text/html; btn-speed swaps aria-pressed for data-active;
  visually hidden marker summary for screen readers; dead CSS swept
  (.panes, .node block, nodePulse, drawCoast, .boot, .marktext,
  .artifact svg, .status-line .dot)
- Claims tests caught prose drift: 111 -> 117 total, decision layer 17;
  DEMO_SCRIPT 3:00 rewritten for the map overlay; "Ninety tests" written
  as a word evaded the digit regex, now durable "Over a hundred tests"

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011fGKoidDHjqXRCPhCZYNEG
