Plain-language pass 2: packed sub, named rulebook, amber band highlight

- Header sub cut to three short sentences; the page below already shows the
  sources, so the enumeration is gone
- Quotes section names its source plainly before any quote appears ("The
  rulebook: the FDA's Guide for the Control of Molluscan Shellfish, 2023
  edition, 532 pages") and the verify line drops "exact substrings" for
  "found word for word on the page they cite. A paraphrase would fail."
- The straddled band is now statically highlighted in warn amber with a
  border and an in-band label, "the closure line lands in this band".
  Amber, not red: the page measures nothing in real time, so red would
  claim danger it cannot support; amber marks ambiguity, which is the
  actual finding
- Hydrophone made concrete and plain: "One underwater microphone (a
  hydrophone), 60 seconds of ocean"; lanes relabeled "EVERYTHING THE
  MICROPHONE HEARD" and "DEEP RUMBLE ONLY, WHERE ENGINES SHOW UP"; the
  note now opens with the station: one microphone, FK04, Florida Keys
  sanctuary
- Demo script removed from the public repo (gitignored): it is stage
  direction and a do-not-say list, backstage material, not project
  documentation. Claims tests keep covering the local copy; every asserted
  string also lives in README or DEVPOST, so a fresh clone stays green

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_011fGKoidDHjqXRCPhCZYNEG
