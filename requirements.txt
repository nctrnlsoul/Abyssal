# Development and the live agent path.
# Pins match the set already proven working on Python 3.13.15 in the previous
# build, so this is not a fresh dependency-resolution gamble two days out.
google-adk==2.7.1
fastapi==0.141.1
uvicorn==0.46.0
pytest==8.3.4
